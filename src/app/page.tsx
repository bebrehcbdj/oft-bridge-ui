import { SplashOverlay } from '@/ui/components/SplashOverlay'

/** The welcome screen: the glass, over the bridge the layout has already mounted behind it. */
export default function Home() {
  return <SplashOverlay />
}
